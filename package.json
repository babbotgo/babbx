{
  "name": "babbbot",
  "version": "1.0.0",
  "description": "Bat bot",
  "main": "src/index.js",
  "scripts": {
    "start": "npm run build && node ./build/index.js",
    "clean": "rm -rf build node_modules && npm install && rm -rf build && mkdir build",
    "build-babel": "babel -d ./build ./src -s",
    "build": "npm run clean && npm run build-babel",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/babbotgo/babbx.git"
  },
  "keywords": [
    "babb",
    "bot"
  ],
  "author": "x",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/babbotgo/babbx/issues"
  },
  "homepage": "https://github.com/babbotgo/babbx#readme",
  "dependencies": {
    "babel-preset-env": "^1.7.0",
    "express": "^4.18.2",
    "telegraf": "^3.30.1"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0"
  }
}
